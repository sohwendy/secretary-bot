const test = require('ava');
const alert = require('../../../src/parser/forexnotification');


test('sample', t => {
  const expected = ['a'];
  const actual = ['a'];

  t.deepEqual(expected, actual);
});
